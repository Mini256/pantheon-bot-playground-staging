# Mini256-pantheon-bot-dev-playground

This repository contains a tiny static "Hello, World!" page intended for simple dev / automation validation.

## View locally

Open `index.html` in a browser.

## Run tests

```bash
python3 -m unittest discover -s tests
```
