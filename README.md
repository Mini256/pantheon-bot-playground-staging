# Mini256-pantheon-bot-dev-playground

This repository includes a tiny static "Hello, world!" page.

## Quick start
- Open `index.html` in a browser.
- Run tests:

```bash
python3 -m unittest discover -s tests
```
